[project]
name = "el-ac-pass-back"
version = "0.1.0"
description = "Backend sidecar for El-Ac Pass Printer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fdb>=2.0.4",
    "pyinstaller>=6.19.0",
]